import { Typography, styled } from '@mui/material'

export const ReadmoreButton = styled('span')({
  color: '#009DFF',
  cursor: 'pointer',
})

export const ReadmoreContainer = styled(Typography)({ textAlign: 'justify' })
